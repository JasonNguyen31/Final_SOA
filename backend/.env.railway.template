# ========================================
# RAILWAY DEPLOYMENT ENVIRONMENT TEMPLATE
# ========================================
# Copy this file and fill in your actual values
# Then add these as Environment Variables in Railway Dashboard

# ========================================
# MONGODB ATLAS CONNECTION
# ========================================
# Lấy từ MongoDB Atlas: Database → Connect → Connect your application
# Format: mongodb+srv://username:password@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://genzmobo_admin:YOUR_PASSWORD_HERE@genzmobo-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
DATABASE_NAME=ONLINE_ENTERTAINMENT_PLATFORM

# ========================================
# UPSTASH REDIS CONNECTION
# ========================================
# Lấy từ Upstash Dashboard: Your Database → REST API
# Format: redis://default:TOKEN@host.upstash.io:6379
REDIS_URL=redis://default:YOUR_UPSTASH_TOKEN_HERE@guiding-macaw-12345.upstash.io:6379

# ========================================
# JWT CONFIGURATION
# ========================================
# Tạo JWT_SECRET mạnh bằng lệnh: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_KEY_32_CHARS_OR_MORE
JWT_ALGORITHM=HS256
JWT_ACCESS_EXPIRE_MINUTES=15
JWT_REFRESH_EXPIRE_DAYS=7

# ========================================
# SERVICE CONFIGURATION
# ========================================
# Railway sẽ tự động cung cấp biến PORT
# Không cần set PORT manually
SERVICE_NAME=auth_service

# ========================================
# EMAIL CONFIGURATION (Optional - for production)
# ========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ========================================
# CORS ORIGINS (Thêm Vercel domain sau khi deploy)
# ========================================
# Example: https://genzmobo.vercel.app
CORS_ORIGINS=http://localhost:5173,https://genzmobo.vercel.app

# ========================================
# NOTES:
# ========================================
# 1. Mỗi service trên Railway cần thêm các biến môi trường này
# 2. Chỉ cần thay SERVICE_NAME cho mỗi service:
#    - auth_service
#    - user_service
#    - movie_service
# 3. PORT sẽ tự động được Railway cung cấp ($PORT)
# 4. Nhớ Generate Domain cho mỗi service sau khi deploy
